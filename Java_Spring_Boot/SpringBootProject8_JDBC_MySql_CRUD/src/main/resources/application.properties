spring.application.name=SpringBootProject8_JDBC_MySql_CRUD

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/spring_boot_jdbc
spring.datasource.username=root
# if use xampp than password not set =>
spring.datasource.password=
# if use mysql 8.4 (instaled mysql) than password is root => 
# spring.datasource.password=root
